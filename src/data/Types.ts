type LangKeyType = { [key: string]: number };

type LangColorType = { [key: number]: number };

type ProjectType = {
  title: string;
  img: string;
  description: string;
  url: string;
};

export type { LangKeyType, LangColorType, ProjectType };
